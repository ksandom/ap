# Remove the last waypoint derived values for a particular instance. --removeLastValues=instanceName ~ nav,waypoint
parameters instanceName

unset AP,state,~!Local,instanceName!~,lastTrack-goal
unset AP,state,~!Local,instanceName!~,distance
unset AP,state,~!Local,instanceName!~,beginingDistance
